<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:tools="http://schemas.android.com/tools"
      tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
                android:id="@+id/nav_new_employee"
                android:icon="@android:drawable/ic_input_add"
                android:title="@string/menu_new_employee"/>
        <item
                android:id="@+id/nav_update_employee"
                android:icon="@drawable/ic_menu_manage"
                android:title="@string/menu_update_employee"/>
        <item
                android:id="@+id/nav_delete_employee"
                android:icon="@android:drawable/ic_delete"
                android:title="@string/menu_delete_employee"/>
        <item
                android:id="@+id/nav_detail_employee"
                android:icon="@android:drawable/ic_menu_info_details"
                android:title="@string/menu_detail_employee"/>
    </group>

</menu>
